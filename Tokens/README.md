# Tokens

Machine-readable and human-readable design decisions live here. Templates and components must reference token names rather than hardcoded visual values.

- Purpose: provide one source of truth for visual values.
- Usage: load token files before components or templates.
- Dependencies: `Brand/`.
- Related modules: `Components/`, `Templates/`, `QA/`.
- Version: `2.0.0`.
