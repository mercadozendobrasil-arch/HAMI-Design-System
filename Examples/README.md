# Examples

Only QA-approved examples may be stored here. Examples illustrate a specification; they never replace it.

- Purpose: show approved applications of templates.
- Usage: compare after loading the governing template and rules.
- Dependencies: `Templates/`, `QA/`, `HAMI-Product-Library`.
- Related modules: `Documentation/`.
- Version: `2.0.0-dev`.
